spring:
  application:
    name: hospitalnotificaciones
  datasource:
    url: jdbc:mysql://localhost:3306/notificaciones_db
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8088

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/