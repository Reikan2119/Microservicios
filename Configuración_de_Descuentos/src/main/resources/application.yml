spring:
  application:
    name: configuraciondescuentos
  datasource:
    url: jdbc:mysql://localhost:3306/descuentos_db
    username: root
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8085

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/